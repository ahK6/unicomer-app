import {StyleSheet} from 'react-native';

export const titleLabelStyle = StyleSheet.create({
  label: {
    color: '#055ea9',
    fontSize: 26,
    fontWeight: 'bold',
    textAlign: 'center',
  },
});

export const normalLabelStyle = StyleSheet.create({
  label: {
    fontSize: 16,
    color: 'rgb(128, 128, 128)',
    textAlign: 'center',
  },
});
